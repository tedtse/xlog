module.exports = {
  // 启动指令，默认为 F12
  launchDirect: 123,
  
  // 是否覆盖 window.console，默认为否
  overwriteConsole: false,
  
  // 是否设为全局变量(window.Xlog)，默认为否
  isGlobalParameter: false,

  // 初始是否显示默认为否
  display: false
};